{
  "note": "The ipfs add parameters used to derive the contenthash CID. Without these the CID is not reproducible: the same bytes give a different CID under a different cid-version, wrapping, chunker or hash. Changing any of these changes the CID without changing the site.",
  "cid_params": {
    "cid_version": 1,
    "wrap_with_directory": false,
    "raw_leaves": true,
    "chunker": "size-262144",
    "hash": "sha2-256"
  }
}
